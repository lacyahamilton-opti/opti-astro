fragment OwensCorningHomePage on OwensCorningHomePage {
    _metadata {
        ...PageUrl
        published
        displayName
    }
    HeroArea {
        _metadata {
            types
        }
        ...ContentArea
    }
    MainContentArea {
        _metadata {
            types
        }
        ...ContentArea
    }
    FooterArea {
        _metadata {
            types
        }
        ...ContentArea
    }
    SeoSettings {
        ...PageSeoSettingsProperty
    }
    PageAdminSettings {
        ...PageAdminSettingsProperty
    }
}
